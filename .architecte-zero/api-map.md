# API map
- MV3 messaging connects AlgoQuest to the persistent side panel.
- Express/Postgres broker routes create runs, admit artifacts, return status, retry, cancel, and accept one-time Colab callbacks.
- JSON export/import is recovery, not the successful path.
