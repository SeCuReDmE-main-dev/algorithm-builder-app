# Data map
- Postgres stores run state and receipt versions; callback capabilities are stored only as hashes.
- Extension session storage holds short-lived authentication state.
- Builder owns editable artifacts, never mission progression.
